<UserControl x:Class="DemoApp.SecondaryUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
            xmlns:view="clr-namespace:BackgroundWorkerObservableView;assembly=BackgroundWorkerObservableView"
        
             xmlns:local="clr-namespace:DemoApp"

             mc:Ignorable="d" 
             d:DesignHeight="130" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
     
        </Grid.RowDefinitions>
        <UniformGrid Rows="1">
            <Button Width="50" Margin="5" Content="Run"
                           Command="{Binding RunCommand}"
                CommandParameter="true"
                      >
            </Button>
            <TextBlock Text="{Binding Iterations}">
                
            </TextBlock>
        </UniformGrid>

        <Viewbox Grid.Row="1" >
            <view:WorkerQueueUserControl DataContext="{Binding  WorkerQueueVM}"/>
        </Viewbox>
    </Grid>
</UserControl>
